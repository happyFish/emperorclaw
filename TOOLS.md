# TOOLS.md

## Local repo tools
- Node/Next.js app (`npm run dev`)
- Drizzle ORM (`npm run db:push`, `npm run db:generate`, `npm run db:seed`)
- MCP API routes under `src/app/api/mcp/*`

## Git
- Remote: `git@github.com:josezuma/emperorclaw.git`
- Active feature branch: `feat/p0-mcp-alignment`
